import styled from 'styled-components'

export const BoxWrapper = styled.div`
	text-align: center;
	padding: 2rem;

	a {
		color: var(--red);

		font-family: Secular One;
		font-size: 20px;
		font-weight: 400;
		line-height: 29px;
	}

	.ghost {
		position: absolute;
		bottom: -50px;
		right: 0;
	}
`
