import styled from 'styled-components'

export const SubtitleStyle = styled.h2`
	font-style: italic;
	font-weight: 700;
	font-size: 48px;
	line-height: 60px;

	text-align: center;
	text-shadow: -6px 7px 6px var(--red);

	color: #ffffff;
`
